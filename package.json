{
  "name": "streaming-neuheiten",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "collect": "npm run collect -w data-collector",
    "services": "npm run services -w data-collector",
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "preview": "npm run preview -w frontend"
  }
}
